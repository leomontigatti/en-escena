/**
 * Whose academy the reader is looking at: their own from the portal, or any
 * from the panel. Every roster duplicate guard says `tu academia` or
 * `la academia` on the strength of it.
 */
export type RosterScope = "portal" | "admin";
